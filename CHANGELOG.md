# 更新日志

## 未发布

## 0.4.0 — 2026-09-03

- KiriKiri/KAG、Ren'Py 与 TyranoScript 接入工作台的提取、翻译、部署三步流程，可生成不修改原游戏的独立汉化副本。
- 新增“查缺补漏”：在已有全文结果中识别仍像日文原文的条目，仅重新提交这些内容，并保留可恢复、可审计的修补记录。
- 查缺补漏支持 YU-RIS、KiriKiri、Ren'Py 与 TyranoScript；QLIE 继续使用其专用回包和部署流程。
- 文本引擎部署会校验控制标记和来源文件，遇到仅编译脚本、加密资源或不安全变更时停止，不覆盖原游戏。
- GitHub 主页加入工作台、翻译流程和实际翻译效果展示图；便携版继续使用唯一入口 `Nagi.exe`。

## 0.3.5 — 2026-09-02

- 放大 Windows 便携版 `Nagi.exe` 图标中的人物主体，重新生成 16、24、32、48、64、128 与 256 像素图层，使资源管理器列表中的小图标更清晰。
- 保留 0.3.2 的部署启动依赖修复，以及模型服务对 HTTP 429/5xx、网络中断和异常 JSON 的渐进退避重试。
- 继续以唯一入口 `Nagi.exe` 启动便携版，并验证嵌入图标、中文及空格路径、重复启动和安全退出。

## 0.3.2 — 2026-09-02

- 修复 Windows 便携版第三步生成的游戏启动器无法从部署目录加载 `locale_support.py` 的问题；启动组件不再依赖嵌入式 Python 被 `python313._pth` 限制的模块搜索路径。
- 便携验收新增独立部署目录导入测试，覆盖中文、空格路径和包外脚本目录。

## 0.3.1 — 2026-09-02

- Windows 便携版改为唯一启动入口 `Nagi.exe`，移除包内的 `Start Nagi.vbs` 与 `Start Nagi.cmd`。
- 新入口嵌入由用户提供参考图制作的 Nagi 多尺寸图标，支持中文和空格路径、重复启动与安全退出。
- 发布验收改为直接运行 `Nagi.exe`，并检查旧入口不存在、应用图标可提取、网页头像与图标原图完整。
- 模型接口遇到 HTTP 429/5xx、网络中断或无效 JSON 响应时采用渐进退避重试；不再把 `null` 响应误报为 `NoneType.get`，失败后继续保留已完成的翻译批次。

## 0.3.0 — 2026-09-02

- 修复目录选择窗口无法显示时持续占用请求的问题，并避免重复打开已有的 Nagi 浏览器页面。
- YU-RIS 479 可依据本次提取记录部署用户选择的游戏目录，复制完整运行资源但排除存档目录，不再绑定单一游戏文件名。
- 完善五类引擎的任务恢复、保存目录、独立副本部署和启动入口修复；移动 Nagi 后会同步刷新运行组件与测试版/正式版入口。
- Windows 便携包新增 PNG 与多尺寸 ICO 快捷方式图标，并继续校验和保留网页头像。

## 0.2.0 — 2026-09-01

- 新增 KiriKiri/KAG、Ren'Py、TyranoScript 三种适配，使视觉小说翻译工作流覆盖五类引擎。
- 三种新适配复用既有提取、API 翻译、部分/全文范围、任务恢复和独立可玩副本发布流程；原游戏不修改。
- KiriKiri 支持散装 `.ks` 与标准未加密 XP3；Ren'Py 支持保留 `.rpy` 的发行包；TyranoScript 支持标准场景目录。加密、编译-only 和控制标签损坏会明确停止。

## 0.1.0 — 2026-08-31

- 品牌、Python 包、公开类、命令、页面与启动入口统一为 Nagi。
- 恢复首页、侧栏和对话中的原 PNG 头像，并修复源码、wheel 与便携包遗漏头像的问题。
- 增加 MIT 主许可证、明确发行输入、依赖与运行时哈希锁、第三方声明收集。
- 增加包含网页资源的 wheel、源码初始化和 Windows x64 独立运行时便携构建。
- 增加隐藏窗口启动、服务身份校验、重复启动处理、主动退出、日志与数据目录选择。
- 增加首次使用与 API 费用说明、组件状态、旧配置版本兼容、游戏运行时重新定位。
- 保留旧 `.pico` 状态与 PICO_* 环境变量兼容，旧译文、任务与游戏存档不自动重写。
- 接入指定 QLIE FilePack 3.1 游戏的脚本回包、Unicode 中文显示、独立可玩副本和任务恢复；校验补丁覆盖顺序，未适配版本明确提示。

已知限制：QLIE 可玩部署限《美少女万华镜 -罪与罚的少女-》的已验证原版引擎；YU-RIS 限已适配 Case 六花 479 版本。
开场选取不支持无法解析的入口、分支或动态跳转。便携包不包含 LE、模型或游戏。
Windows DPAPI 配置换电脑/用户后需重新填写密钥。旧路径失效要恢复原路径或按升级说明处理。
首次发布文件尚需维护者在 GitHub 将草稿正式发布；本地构建不等同于公开下载。
